# AWS IRSA overlay — adds IAM role annotation to the service account.
# Used when deploying to EKS with IAM Roles for Service Accounts.
serviceAccount:
  create: true
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::{{AWS_ACCOUNT_ID}}:role/{{PROJECT_NAME}}-role"
